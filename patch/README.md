The executable installer is `tools/tcg/apply_foundation.py`.

It uses exact text anchors and aborts when the inspected Gen1Recomp `dev`
source has changed, instead of applying fuzzy edits. This directory is kept
for future generated git-format patches once the target repository/fork is
fixed to a commit.
